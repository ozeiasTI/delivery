import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    header : {
        alignItems : 'center',
        width: '100%'
    },
    logo : {
       width: 170,
       height: 50
    },
    titulo : {
        color: COLORS.darkgray,
        fontSize: FONT_SIZE.md,
        textAlign: 'center'
    }
}